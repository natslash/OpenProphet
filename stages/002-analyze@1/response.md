I have implemented the `IbkrDataService` skeleton in `services/ibkr_data.go` and verified it satisfies the `interfaces.DataService` interface using a compile-time assertion. I have updated `PROGRESS.md` to mark this step as complete.

Next step (Phase 3.2): Implement `services/ibkr_trading.go` read paths (account, positions, open orders). 

Please confirm this step is complete and ready for the next.